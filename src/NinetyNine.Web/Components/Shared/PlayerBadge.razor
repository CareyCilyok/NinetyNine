@* Player name + avatar combination, links to the player's public profile. *@

<a href="/players/@PlayerId" class="player-badge d-inline-flex align-items-center gap-2 text-decoration-none">
    <AvatarImage AvatarUrl="@AvatarUrl" DisplayName="@DisplayName" SizePx="@AvatarSizePx" />
    <span class="player-badge-name">@DisplayName</span>
</a>

@code {
    [Parameter] public Guid PlayerId { get; set; }
    [Parameter] public string DisplayName { get; set; } = "";
    [Parameter] public string? AvatarUrl { get; set; }
    [Parameter] public int AvatarSizePx { get; set; } = 32;
}
